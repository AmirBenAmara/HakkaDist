{% extends 'base.html.twig' %}

{% block body %}


    <div class="container">
        <div class="row">
            <div class="col-md-5">
                <h1><b class="label label-default">Film:</b> {{ bordereauRegion.film.titre }}</h1>
            </div>
            <div class="col-md-5">
                <h1><b class="label label-default">Region:</b> {{ bordereauRegion.region.nom }}</h1>
            </div>
            <div class="col-md-2">
                <h1> <a href="{{ path('brdsalle_edit', { 'id': bordereauRegion.id }) }}">   <span class=" glyphicon glyphicon-pencil" aria-hidden="true"></span></a></h1>
            </div>
            <div class="col-md-5">
                <h1><b class="label label-default">Nb Entrées:</b> 0</h1>
            </div>
            <div class="col-md-5">
                <h1><b class="label label-default">Recette:</b> 0</h1>
            </div>
            <div class="col-md-2">
                <h1> <span class="text-danger glyphicon glyphicon-remove " aria-hidden="true"></span></h1>
            </div>

        </div>
    </div>
    <hr>


    <br>
    <a style="float:right" class="btn btn-primary" href="{{ path('seanceregion_new' , { 'id': bordereauRegion.id }) }}">Ajouter une seance</a>

    <table class="table table-striped ">
        <thead class="thead-dark">
        <tr><h3>Liste des seances</h3></tr>
        <tr>
            <th>Nbr_entree_seance</th>
            <th>Recette_seance</th>
            <th>Date_seance</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for seanceSalle in bordereauRegion.seances %}
            <tr>
                <td>{{ seanceSalle.nbrentreeseance }}</td>
                <td>{{ seanceSalle.recetteseance }}</td>
                <td>{% if seanceSalle.dateseance %}{{ seanceSalle.dateseance|date('Y-m-d') }}{% endif %}</td>
                <td>

                    <a class="btn btn-info" href="{{ path('seancesalle_show', { 'id': seanceSalle.id }) }}">afficher</a>


                    <a class="btn btn-success" href="{{ path('seancesalle_edit', { 'id': seanceSalle.id }) }}">modifier</a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>






    {#<ul>#}
    {#<li>#}
    {#<a href="{{ path('brdsalle_index') }}">Back to the list</a>#}
    {#</li>#}
    {#<li>#}
    {#<a href="{{ path('brdsalle_edit', { 'id': bordereauRegion.id }) }}">Edit</a>#}
    {#</li>#}
    {#<li>#}
    {#{{ form_start(delete_form) }}#}
    {#<input type="submit" value="Delete">#}
    {#{{ form_end(delete_form) }}#}
    {#</li>#}
    {#</ul>#}
{% endblock %}