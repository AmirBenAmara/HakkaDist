{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bordereauregion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bordereauRegion.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bordereauregion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bordereauregion_edit', { 'id': bordereauRegion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
